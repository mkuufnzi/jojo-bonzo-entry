import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { console.log('Checking unified charts direct from DB...'); const bId = '181a05e7-66b0-47e0-94fe-1f7bbaaca735'; try { const invoices = await (prisma as any).unifiedInvoice.findMany({ where: { businessId: bId, status: { notIn: ['VOIDED', 'DELETED'] } }, select: { amount: true, issuedDate: true } }); console.log('Trend invoices:', invoices.length); } catch (e: any) { console.error('Prisma Error:', e.message); } } main().catch(console.error).finally(()=>prisma.$disconnect());
