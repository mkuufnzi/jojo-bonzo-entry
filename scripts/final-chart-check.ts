import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { const bId = '181a05e7-66b0-47e0-94fe-1f7bbaaca735'; console.log('Checking Top Customers and SalesBySource directly...'); const invoices = await (prisma as any).unifiedInvoice.findMany({ where: { businessId: bId, status: { notIn: ['VOIDED', 'DELETED'] } }, select: { amount: true, source: true }}); console.log('Valid invoices for charts:', invoices.length); } main().catch(console.error).finally(()=>prisma.$disconnect());
