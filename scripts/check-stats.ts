import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { console.log('Checking unified stats...'); const inv = await prisma.unifiedInvoice.findMany({take:5}); console.log('Invoices sample:', inv.length); const stats = await prisma.unifiedInvoice.aggregate({ _sum: { amount: true }, _count: { id: true } }); console.log('Agg:', stats); } main().catch(console.error).finally(()=>prisma.$disconnect());
