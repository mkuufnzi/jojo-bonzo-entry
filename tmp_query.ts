import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { const businesses = await prisma.business.findMany({ include: { users: true, integrations: true }}); console.log(JSON.stringify(businesses, null, 2)); const invoices = await prisma.unifiedInvoice.findMany(); console.log('Unified Invoices:', invoices.length); } main().finally(() => prisma.$disconnect());