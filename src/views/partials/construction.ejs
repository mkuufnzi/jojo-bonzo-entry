<div class="flex-1 flex flex-col items-center justify-center p-12 text-center text-slate-400">
    <div class="mb-4 bg-slate-50 p-4 rounded-full">
        <i data-lucide="cone" class="w-8 h-8 text-slate-300"></i>
    </div>
    <h3 class="text-sm font-black uppercase tracking-widest text-slate-500 mb-1"><%= locals.title || 'Under Construction' %></h3>
    <p class="text-xs font-medium max-w-xs mx-auto"><%= locals.message || 'This section is currently being built.' %></p>
</div>
