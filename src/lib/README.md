# Libraries & Utilities

This directory contains shared libraries, configuration instances, and utility helpers used across the application.

## Files

-   `prisma.ts`: Exports the singleton instance of the `PrismaClient`. It handles the initialization of the Prisma Client with the LibSQL adapter for SQLite support.
