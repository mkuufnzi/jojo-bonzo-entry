# Schemas

This directory contains Zod schemas used for request validation. These schemas ensure that the data entering the application matches the expected format and types.

## Files

-   `pdf.schema.ts`: Defines the validation schema for the PDF conversion request body (`ConvertPdfSchema`), including source type (URL/HTML) and Puppeteer options.
